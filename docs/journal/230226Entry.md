February 23, 2026

Today I started working on the frontend of the dispatch stack. I created a new Next.js app with the help of AI and installed the necessary dependencies. While creating the directories, I realized that the AI was generating too many directories and was not following the structure that I wanted. I had the AI delete unnecessary directories and create the necessary files to start the frontend. Upon my first run of the app, I noticed there were outdated dependencies that needed to be updated. I used an override in package.json to update the dependencies. Finally, I learned the fundamentals of Git, specifically how to commit and push to the main branch.